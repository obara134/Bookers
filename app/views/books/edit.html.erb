<h1><strong>Editing Book</strong></h1>
 <%= form_for @book do |f| %>
  <% if @book.errors.any? %>
    <h3><%= @book.errors.count %>error prohibited this book from being saved:</h3>
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  <% end %>
  <% end %>

<%= form_with model:@book, local: true do |f| %>
 <h4>Title</h4>
 <%= f.text_field :title %>
 <h4>Body</h4>
 <%= f.text_area :body %>
 <br><%= f.submit 'Update Book' %></br>

 <p><br><%= link_to "Show", book_path(@book.id) %> | <%= link_to "Back", new_book_path %>
 </p>


<% end %>

